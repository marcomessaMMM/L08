[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "fc_Messa"
authors = [
    {name = "Marco Maria Messa", email = "marco.messa@inaf.it"},
]
description = "Creates a finding chart from coordinates"
version = "0.1.0"
dependencies = []